# R MCP Server (Dockerized)

Exposes the btw MCP server and custom R tools via Docker for isolated, secure execution.

**Note:** You must register this MCP server with Claude Desktop and/or Claude Code (CLI) to use it. See `r-playground-using-mcp` for examples.
